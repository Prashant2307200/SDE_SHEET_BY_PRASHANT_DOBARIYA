const int mod = 1e9+7;
int findWays(vector<int>& arr, int k) {
	int n = arr.size();
	vector<int>dp (k+1, 0);
	dp[0] = 1;
	for(int i = 1; i <= n; ++i) {
		for(int j = k; j >= 0; --j) {
			if(arr[i-1] <= j)
				dp[j] = (dp[j] + dp[j-arr[i-1]])%mod;
		}
	}
	return dp[k];
}